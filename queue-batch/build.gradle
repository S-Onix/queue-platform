apply plugin: 'org.springframework.boot'

dependencies {
    implementation project(':queue-domain')
    implementation project(':queue-infrastructure')
    implementation project(':queue-common')

    // Spring MVC (Batch도 내장 Tomcat)
    implementation 'org.springframework.boot:spring-boot-starter-web'

    // @Transactional(spring-tx): 이전엔 spring-kafka가 전이 제공 → Kafka 제거로 명시 추가
    // (TokenEnqueueService가 outbox 드레인 적재를 트랜잭션으로 처리)
    implementation 'org.springframework:spring-tx'

    // E2E/부하 테스트에서 JPA·Redis 클래스 컴파일 참조용 (infra의 것들은 implementation이라 미노출)
    testImplementation 'org.springframework.boot:spring-boot-starter-data-jpa'
    testImplementation 'org.springframework.boot:spring-boot-starter-data-redis'

    compileOnly      "org.projectlombok:lombok:${lombokVersion}"
    annotationProcessor "org.projectlombok:lombok:${lombokVersion}"
}