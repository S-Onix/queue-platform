apply plugin: 'org.springframework.boot'

dependencies {
    implementation project(':queue-domain')
    implementation project(':queue-infrastructure')
    implementation project(':queue-common')

    // TokenReclaimJob이 EXPIRED를 발행한다(§80). 어댑터는 infra에 있지만 KafkaTemplate
    // 오토컨피그가 이 모듈의 런타임 클래스패스에 있어야 하므로 전이 의존에 기대지 않고 명시한다
    // (queue-consumer의 spring-tx 주석과 같은 이유 — 조용히 사라지면 발행 경로가 통째로 죽는다).
    implementation 'org.springframework.kafka:spring-kafka'

    // Spring MVC (Batch도 내장 Tomcat)
    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation 'org.springframework.boot:spring-boot-starter-actuator'

    // 이게 없으면 레지스트리 빈이 없어 /actuator/prometheus 엔드포인트 자체가 생기지 않는다.
    // application.yml이 prometheus를 노출해도 404 — 스케줄 작업의 처리 건수를 볼 수단이 사라진다.
    implementation 'io.micrometer:micrometer-registry-prometheus'

    // @Transactional(spring-tx). infra가 전이 제공하지만 명시한다 —
    // 스케줄 작업(Sprint 9)이 트랜잭션을 직접 쓰게 되므로 남의 의존성에 기대지 않는다.
    implementation 'org.springframework:spring-tx'

    // 통합 테스트에서 JPA·Redis 클래스 컴파일 참조용 (infra의 것들은 implementation이라 미노출)
    testImplementation 'org.springframework.boot:spring-boot-starter-data-jpa'
    testImplementation 'org.springframework.boot:spring-boot-starter-data-redis'

    compileOnly      "org.projectlombok:lombok:${lombokVersion}"
    annotationProcessor "org.projectlombok:lombok:${lombokVersion}"
}