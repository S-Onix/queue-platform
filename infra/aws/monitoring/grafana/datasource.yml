# 🔑 uid 를 prometheus 로 고정한다. 대시보드 JSON 이 ${DS_PROMETHEUS} 를 쓰는 export 판이라
#    deploy.sh 가 그 문자열을 이 uid 로 치환해서 넣는다.
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    url: http://localhost:9090
    isDefault: true
